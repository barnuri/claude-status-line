{
  "name": "claude-status-line",
  "version": "1.0.0",
  "description": "Claude Code status line — shows cwd, model, context%, tokens, and rate-limit stats",
  "bin": {
    "claude-status-line": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "setup": "bun run src/index.ts --setup",
    "capture": "bun run scripts/preview.ts && bun run scripts/animated-preview.ts && bun run scripts/make-gif.ts"
  },
  "keywords": ["claude", "claude-code", "status-line"],
  "author": "barnuri",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "gifenc": "^1.0.3",
    "pngjs": "^7.0.0"
  }
}
